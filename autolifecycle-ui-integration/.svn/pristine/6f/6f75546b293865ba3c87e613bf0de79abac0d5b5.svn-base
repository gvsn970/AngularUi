import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListOfReleasesComponent } from './list-of-releases.component';

describe('ListOfReleasesComponent', () => {
  let component: ListOfReleasesComponent;
  let fixture: ComponentFixture<ListOfReleasesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListOfReleasesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListOfReleasesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

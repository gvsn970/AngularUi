import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ImportFeaturesComponent } from './import-features.component';

describe('ImportFeaturesComponent', () => {
  let component: ImportFeaturesComponent;
  let fixture: ComponentFixture<ImportFeaturesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ImportFeaturesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ImportFeaturesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

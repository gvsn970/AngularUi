import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SyncReleasesComponent } from './sync-releases.component';

describe('SyncReleasesComponent', () => {
  let component: SyncReleasesComponent;
  let fixture: ComponentFixture<SyncReleasesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SyncReleasesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SyncReleasesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
